console.log("Starting notes...");

const addNote = (title, body) => {
  console.log(title, body);
};

const getAll = (title, body) => {
  console.log(title, body);
};

module.exports = {
  addNote
};
